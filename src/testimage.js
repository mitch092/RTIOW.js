// TODO: make the test image. Refactor this to use unfold + yield instead of for loops (maybe).
const draw = canvas => {
    for(let i = 0; i != canvas.getHeight(); ++i){
        for(let j = 0; i != canvas.getWidth(); ++j){

        }
    }
}